[project]
name = "python-code-metrics-analyzer"
version = "0.0.1"
description = "Helper to compute Python code metrics for PyLens"
dependencies = [
    "radon"
]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"